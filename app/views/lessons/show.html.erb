<h1><%= @lesson.title %></h1>
<p><%= @lesson.description %></p>

<div class="my-4 p-5 bg-light border rounded">
  <p class="text-center">(Nội dung bài học/Video sẽ hiển thị ở đây)</p>
  </div>

<hr>

<%# 1. Kiểm tra xem bài học này có bài quiz không %>
<% if @lesson.quizzes.any? %>
  <%# (Chúng ta giả định mỗi lesson chỉ có 1 quiz) %>
  <% quiz = @lesson.quizzes.first %>

  <div class="card bg-light text-center p-4">
    <div class="card-body">
      <h4 class="card-title">Bài kiểm tra: <%= quiz.title %></h4>
      <p class="card-text"><%= quiz.description %></p>

      <%# 2. Dùng 'button_to' (nó sẽ gửi 'POST') %>
      <%= button_to "Bắt đầu làm bài",
            quiz_quiz_attempts_path(quiz),
            method: :post,
            class: "btn btn-primary btn-lg" %>

      <small class="text-muted d-block mt-2">
        Thời gian: <%= quiz.time_limit || "Không giới hạn" %> phút.
        Điểm đậu: <%= quiz.pass_score %>%
      </small>
    </div>
  </div>
<% end %>
<hr>
<%= link_to "Quay lại Khóa học", course_path(@lesson.course), class: "btn btn-outline-secondary mt-3" %>
